# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*

# Data & experiments (runtime artifacts)
data/
experiments/
*.parquet

# Tooling
.ruff_cache/
.mypy_cache/
.pytest_cache/
htmlcov/
.coverage
dist/
*.egg-info/
uv.lock

# References package archives (source trees are tracked; zips are local-only)
References/*.zip
References/**/Package-*-Overview.docx

# Athena package archives (source trees are tracked; zips are local-only)
athena/**/*.zip
