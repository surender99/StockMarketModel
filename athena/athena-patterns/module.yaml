name: athena-patterns
owner: patterns-team
bounded_context: patterns
version: "0.1.0"
description: Pattern recognition bounded context
dependencies:
  packages:
    - athena-os
    - athena-common
    - athena-core
events:
  publishes:
    - PatternDetected
  consumes:
    - IndicatorCalculated
interfaces:
  - athena_domain.contracts.protocols.PatternEngine
api:
  modules:
    - athena_patterns
  openapi: null
database:
  schemas: []
quality:
  fitness_tests: true
  min_coverage: 0.0
