[project]
name = "athena-research"
version = "0.1.0"
description = "Research workspace — separate from production runtime"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Athena Contributors" }]

dependencies = [
    "athena-os @ file:../athena-os",
    "athena-common @ file:../athena-common",
    "athena-core @ file:../athena-core",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/athena_research"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"
