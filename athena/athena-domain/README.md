# athena-domain

Domain interface contracts (Python `Protocol`) for bounded contexts. Implementations live in `athena-indicators`, `athena-patterns`, etc. and delegate to `athena-core` during the facade extraction phase.

See [ADR-0006](../athena-spec/adrs/ADR-0006-bounded-contexts.md).
