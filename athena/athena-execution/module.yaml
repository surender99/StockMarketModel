name: athena-execution
owner: execution-team
bounded_context: execution
version: "0.1.0"
description: Simulation and execution bounded context
dependencies:
  packages:
    - athena-os
    - athena-common
    - athena-core
events:
  publishes:
    - SimulationOrder
  consumes:
    - SignalGenerated
interfaces:
  - athena_domain.contracts.protocols.ExecutionEngine
api:
  modules:
    - athena_execution
  openapi: null
database:
  schemas:
    - orders
    - trades
quality:
  fitness_tests: true
  min_coverage: 0.0
