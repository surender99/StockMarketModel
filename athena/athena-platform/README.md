# athena-platform

Executable product assembly layer — wires `athena-os` infrastructure with `athena-core` and bounded-context facades for production startup.

## Usage

```bash
python -m athena_platform --list-features
athena-platform --config path/to/config.yaml
```
